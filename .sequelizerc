const path = require('path')

module.exports = {
  'config': path.resolve('src/migrate-db/config', 'config'),
  'models': path.resolve('src/migrate-db', "..",'models'),
  'seeders': path.resolve('src/migrate-db',"..",'seeders'),
  'migrations': path.resolve('src/migrate-db/', "..", 'migrations')
}